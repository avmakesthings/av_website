<div class="project-nav">

  {{#categories}}

    <div>
    	<a data-target="{{slug}}" class="category-link"> >> {{name}}</a>
	    <div class="category {{slug}}">

        {{#projects}}

  		    <div><a href="{{slug}}.html"> - {{name}}</a></div>
          
  		  {{/projects}}

	    </div>
    </div>

  {{/categories}}

</div>
